DATABASE_URL=postgresql://user:password@host:5432/dbname
CORS_ORIGINS=https://<your-project>.vercel.app
MISTRAL_API_KEY=your-mistral-api-key
# Active la simulation de date (voir dev_date.py) : sans elle, le backend ignore le header
# X-Dev-Date même si le panneau Mode développeur est ouvert côté frontend (?devmode=1).
DEV_MODE=true
